<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.integrador.adm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.CadastroCDActivity"></activity>
        <activity android:name=".Activitys.CupomdescontoActivity" />
        <activity android:name=".Activitys.CadastroTActivity" />
        <activity android:name=".Activitys.TimeActivity" />
        <activity android:name=".Activitys.CadastroTPActivity" />
        <activity android:name=".Activitys.TipoprodutoActivity" />
        <activity android:name=".Activitys.CadastroLActivity" />
        <activity android:name=".Activitys.LigaActivity" />
        <activity android:name=".Activitys.CadastroCActivity" />
        <activity android:name=".Activitys.CargoActivity" />
        <activity android:name=".Activitys.CadastroMActivity" />
        <activity android:name=".Activitys.MarcaActivity" />
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>